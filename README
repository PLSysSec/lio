
* Pre-requisites.  Before building this code, you must use the
  following cabal commands to install required libraries:

	cabal update
	cabal install hscolour
	cabal install SHA
	cabal install cereal
	cabal install directory filepath base64-bytestring
	cabal install dclabel

* Documentation.  The documentation must be extracted from the source
  code using 'haddock'.  Run the following commands to build and view
  the documentation:

	gmake browse

